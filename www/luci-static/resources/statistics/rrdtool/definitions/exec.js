'use strict';'require baseclass';return baseclass.extend({title:_('Exec'),rrdargs(graph,host,plugin,plugin_instance,dtype){const rv=[];const types=graph.dataTypes(host,plugin,plugin_instance);if(types.indexOf('fanspeed')>-1){rv.push({per_instance:true,title:"%H: %pi - %di",vlabel:"rpm",number_format:"%4.0lf rpm",data:{types:["fanspeed"],options:{fanspeed__value:{color:"0000ff",title:"Fan speed"}}}});}return rv;}});
